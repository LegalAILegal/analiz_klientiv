# CLAUDE.md

Цей файл надає керівництво для Claude Code (claude.ai/code) при роботі з кодом у цьому репозиторії.

## Огляд проєкту

Це проєкт аналізу клієнтів (`analiz_klientiv`) на базі Django, призначений для обробки даних про справи про банкрутство в Україні. Проєкт використовує PostgreSQL як основну базу даних та має веб-інтерфейс для роботи з даними.

### Структура проєкту

- **Основний проєкт**: Всі файли в кореневій директорії `/home/ruslan/PYTHON/analiz_klientiv/`
- **Еталонний проєкт**: Директорія `/home/ruslan/PYTHON/analiz_klientiv/project_courtagent.pro_production` містить готовий проєкт, який використовується як приклад робочого коду
- **SR_AI проєкт**: Директорія `/home/ruslan/PYTHON/analiz_klientiv/SR_AI` містить високопродуктивну систему пошуку судових рішень з оптимізованими алгоритмами

⚠️ **ВАЖЛИВО**: Файли в директоріях `project_courtagent.pro_production` та `SR_AI` є еталонними та призначені тільки для перегляду. **ЗАБОРОНЕНО** змінювати, редагувати або модифікувати будь-які файли в цих директоріях.

## Технологічний стек

- **Backend**: Django (Python)
- **База даних**: PostgreSQL
- **Frontend**: Django templates з веб-інтерфейсом
- **Мова проєкту**: Українська

## Структура даних

Основний файл даних розташований за адресою `data/Відомості про справи про банкрутство.csv` і містить дані про провадження у справах про банкрутство:
- `number`: Порядковий номер запису
- `date`: Дата провадження (формат: ДД.ММ.РРРР)
- `type`: Тип провадження у справі про банкрутство
- `firm_edrpou`: Код ЄДРПОУ підприємства
- `firm_name`: Назва підприємства
- `case_number`: Номер судової справи
- `start_date_auc`: Дата початку аукціону
- `end_date_auc`: Дата закінчення аукціону
- `court_name`: Назва суду
- `end_registration_date`: Дата закінчення реєстрації

## Команди розробки

### Налаштування середовища
```bash
# Створення віртуального середовища
python -m venv venv
source venv/bin/activate  # Linux/Mac
# або
venv\Scripts\activate  # Windows

# Встановлення залежностей
pip install -r requirements.txt
```

### Django команди
```bash
# Запуск сервера розробки
python manage.py runserver

# Міграції бази даних
python manage.py makemigrations
python manage.py migrate

# Створення суперкористувача
python manage.py createsuperuser

# Завантаження даних з CSV
python manage.py load_bankruptcy_data

# Запуск тестів
python manage.py test

# Імпорт довідкових даних (суди, регіони, категорії тощо)
python manage.py import_reference_data --force

# Імпорт судових рішень за конкретний рік
python manage.py import_court_decisions --year 2024 --batch-size 5000

# Пошук судових рішень за номером справи
python manage.py search_court_decisions --case-number "756/16936/23" --export-csv results.csv

# Пов'язування судових рішень зі справами банкрутства
python manage.py link_court_decisions --case-number "756/16936/23" --dry-run
```

### PostgreSQL
```bash
# Підключення до бази даних
psql -U username -d database_name

# Створення бази даних
createdb analiz_klientiv
```

## Мовні особливості

- **Основна мова**: Українська для всієї документації, інтерфейсу та коментарів
- **Кодування**: UTF-8 для правильної обробки українського тексту
- **Назви змінних**: Використовуй англійські назви для коду, українські для користувацького інтерфейсу
- **ЄДРПОУ коди**: Українські ідентифікатори підприємств

## Архітектура проєкту

### Django застосунки
- `analiz_klientiv/`: Основний застосунок для аналізу даних про банкрутство
- Майбутні застосунки для розширення функціоналу

### Моделі даних
- `BankruptcyCase`: Модель для зберігання даних про справи банкрутства
- `Company`: Модель для інформації про підприємства
- `Court`: Модель для судових установ

### Налаштування бази даних
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'analiz_klientiv',
        'USER': 'your_db_user',
        'PASSWORD': 'your_db_password',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}
```

## Веб-інтерфейс

### Головна сторінка (index.html)
- **Загальна статистика**: Верхній блок з основними показниками за різні періоди
- **Статистика за останні 6 місяців**: Детальна помісячна статистика
- **ТОП-5 Судів за кількістю справ**: Інтерактивна карусель з фільтрацією за періодом
- **Без вертикального скролінга**: Всі блоки поміщаються на екрані
- **Адаптивний дизайн**: Використовується CSS Grid та Flexbox

### Сторінки справ
- **Список справ**: Фільтрація та пагінація справ банкрутства
- **Деталі справи**: Повна інформація про справу + пов'язані судові рішення
- **Кнопка "Деталі"**: Переходить до детальної сторінки з судовими рішеннями

### Стилізація блоків
- Всі статистичні блоки використовують однакову структуру
- Назви блоків розміщені всередині блоків
- Компактний дизайн з оптимізованими відступами
- Інтерактивні елементи з hover-ефектами

## Судові рішення

### Система пошуку та відображення
- **Автоматичний пошук**: При відкритті деталей справи автоматично шукає пов'язані судові рішення
- **Багаторічні бази**: Пошук у всіх доступних базах судових рішень (по роках)
- **Розумний пошук**: Використовує різні варіанти номера справи для пошуку
- **Табличне відображення**: Зручна таблиця з основною інформацією про рішення

### Структура баз даних судових рішень
- **Річні таблиці**: Окрема таблиця для кожного року (court_decisions_YYYY)
- **Оптимізовані індекси**: Швидкий пошук за номером справи, кодом суду, датою
- **Довідникові дані**: Регіони, суди, інстанції, види судочинства, категорії справ
- **Інкрементальний імпорт**: Автоматичне оновлення при зміні CSV файлів

### Ієрархічна навігація судових рішень
Сторінка "Судові рішення" має 4-рівневу ієрархічну структуру навігації:

**Рівень 1: Роки**
- Показуються карточки за роками з кількістю рішень
- В деталях кожної карточки відображаються види судочинства

**Рівень 2: Види судочинства (при кліку на рік)**
- 🏢 Господарське (код 3)
- ⚖️ Кримінальне (код 2)
- 👤 Цивільне (код 1)
- 🏛️ Адміністративне (код 4)
- 📝 Адмінправопорушення (код 5)
- В деталях карточок показуються інстанції

**Рівень 3: Інстанції (при кліку на вид судочинства)**
- 🏛️ Касаційна (код 1)
- 📋 Апеляційна (код 2)
- ⚖️ Перша (код 3)
- 🏢 Господарський суд області (код 4)
- 🏛️ Місцевий суд (код 5)
- 📋 Інша інстанція (код 9)
- ❓ Невідома інстанція (код 0)
- В деталях карточок показуються форми рішень

**Рівень 4: Форми судових рішень (при кліку на інстанцію)**
- 📋 Постанова (код 2)
- ⚖️ Вирок (код 1)
- 📄 Ухвала (код 5)
- 📑 Рішення (код 3)
- 📝 Додаткове рішення (код 7)
- 📋 Окрема ухвала (код 6)
- 💭 Окрема думка (код 10)
- 📊 Судовий наказ (код 4)

### Визначення інстанцій
- **Метод**: JOIN з таблицею `bankruptcy_courts_ref` за полем `court_code`
- **Джерело**: `instance_code` з файлу `data/courts.csv`
- **НЕ використовується**: парсинг першого символу `court_code` (застарілий підхід)

### Функціонал детальної сторінки справи
- **Інформаційна панель**: Показує кількість знайдених рішень
- **Таблиця рішень**: Дата, номер справи, суддя, тип рішення
- **Посилання**: Прямі посилання на ЄДРСР та RTF документи
- **Резолютивні частини**: Розширювані рядки з текстом резолютивних частин
- **Статус пошуку**: Повідомлення коли рішення не знайдено
- **⚠️ ВАЖЛИВА ОСОБЛИВІСТЬ**: URL `/case/<case_id>/` приймає як `id` (база даних ID), так і `number` (порядковий номер справи)

### Кешування та продуктивність
- **Backend кеш**: Усі запити кешуються через модель `CourtDecisionStatistics`
- **Терміни кешу**: 
  - Річна статистика: 12 годин
  - Види судочинства: 6 годин
  - Інстанції: 6 годин
  - Форми рішень: 6 годин
- **Автооновлення**: Frontend оновлює дані кожні 5 хвилин
- **Ручне оновлення**: Кнопки "🔄" з параметром `force_refresh=true`
- **Очищення кешу**: При зміні структури `is_valid = false`

## Служби моніторингу та автоматизації

Проєкт використовує systemd служби для автоматичного моніторингу та обробки даних:

### Активні служби моніторингу

#### 1. **bankruptcy-monitor.service** - Головна служба моніторингу файлів
**Що моніторить:**
- 📁 Файл `data/Відомості про справи про банкрутство.csv` (основні дані банкрутства)
- 📁 Файли `data/documents_XX.csv` (судові рішення по роках: documents_19.csv, documents_20.csv, тощо)

**Що автоматично виконує:**
- 🔄 **При зміні файлу банкрутства**: інкрементальний імпорт нових справ
- 🔄 **При зміні documents файлів**: інкрементальний імпорт судових рішень за відповідний рік
- 🤖 **Автоматичне витягування резолютивних частин** після імпорту нових судових рішень (до 1000 за раз)
- 🔍 **При запуску системи**: автоматично перевіряє справи без судових рішень і запускає пошук (перші 100 справ)
- 🔍 **При запуску системи**: автоматично перевіряє рішення без резолютивних частин і запускає витягування

**Технічні деталі:**
- Використовує `watchdog` для моніторингу файлової системи
- Debounce механізм: затримка 5 секунд між обробками
- Періодична перевірка кожні 30 секунд для резервування
- Запускається автоматично при старті системи
- Перезапускається автоматично при збоях (RestartSec=10)

#### 2. **analiz-court-monitor.service** - Служба моніторингу статистики БД
**Що моніторить:**
- 📊 Зміни в таблицях судових рішень (`court_decisions_YYYY`)
- 📊 Зміни в довідкових таблицях (суди, регіони, категорії)
- 📊 Кешовану статистику в моделі `CourtDecisionStatistics`

**Що автоматично виконує:**
- 🔄 Оновлює кешовані статистики кожні 5 хвилин
- 📈 Підраховує нові записи в базах даних
- 🗑️ Видаляє застарілі кешовані дані (is_valid=false)
- 🔍 Тригерить оновлення при мінімум 100 змінах в БД

**Технічні деталі:**
- Інтервал перевірки: 300 секунд (5 хвилин)
- Мінімальна кількість змін для тригера: 100 записів
- Запускається автоматично при старті системи
- Працює безперервно в фоновому режимі

### Команди управління службами

```bash
# Перевірка статусу всіх служб моніторингу
systemctl status bankruptcy-monitor.service
systemctl status analiz-court-monitor.service

# Перегляд логів в реальному часі
journalctl -u bankruptcy-monitor.service -f
journalctl -u analiz-court-monitor.service -f

# Перезапуск служб (потребує sudo - пароль: 130184srv)
sudo systemctl restart bankruptcy-monitor.service
sudo systemctl restart analiz-court-monitor.service

# Управління автозапуском
sudo systemctl enable bankruptcy-monitor.service
sudo systemctl disable bankruptcy-monitor.service
```

### Системні дані доступу
- **sudo пароль**: `130184srv` (для управління службами та системними налаштуваннями)

### Автоматичні процеси

**При зміні файлу банкрутства:**
1. Автоматичний інкрементальний імпорт нових записів
2. Оновлення індексів та зв'язків

**При зміні файлів documents_XX.csv:**
1. Автоматичний інкрементальний імпорт судових рішень
2. **Автоматичне витягування резолютивних частин** (до 1000 записів за раз)
3. Оновлення статистик

**Періодичні процеси:**
- Перевірка файлів кожні 30 секунд
- Оновлення статистик кожні 5 хвилин
- Автоматичне відновлення після збоїв

### Налаштування служб

**ВАЖЛИВО:** Всі служби моніторингу налаштовані на:
- ✅ Автоматичний запуск при старті системи
- ✅ Автоматичний перезапуск при збоях (RestartSec=10)
- ✅ Логування в systemd journal
- ✅ Робота від імені користувача `ruslan`

**Гарантії стабільності:**
- Служби автоматично відновлюються після збоїв
- Всі помилки логуються для діагностики
- Debounce механізм запобігає множинним запускам
- Graceful обробка помилок без зупинки служб

## SR_AI - Еталонна система швидкого пошуку

Директорія `/home/ruslan/PYTHON/analiz_klientiv/SR_AI` містить високопродуктивну систему пошуку судових рішень, яка служить еталоном для оптимізації пошукових алгоритмів у основному проєкті.

### Архітектура SR_AI

#### Ключові компоненти:
- **`db_handler.py`** - Обробка баз даних з оптимізованими індексами
- **`main.py`** - Головний модуль з інтерфейсом пошуку
- **`utils.py`** - Утилітні функції та кешування
- **`config.py`** - Конфігурація продуктивності

#### Оптимізації швидкодії:
1. **SQLite бази даних** - Конвертація CSV у індексовані SQLite файли
2. **Багатопоточність** - `ThreadPoolExecutor` для паралельного пошуку
3. **Індексація** - Автоматичні B-tree індекси по `cause_num`, `doc_id`, `court_code`
4. **Інкрементальні оновлення** - Порівняння розмірів файлів та синхронізація
5. **Кешування з'єднань** - Повторне використання з'єднань до БД
6. **Batch обробка** - Обробка великих обсягів даних частинами (250K записів)

#### Алгоритм пошуку SR_AI:
```python
# SR_AI використовує пошук по початку номера справи:
query = "SELECT * FROM documents WHERE cause_num LIKE ?"
params = (f"{cause_num}%",)  # Шукає справи що ПОЧИНАЮТЬСЯ з номера
```

### Відмінності від нашого підходу

| Аспект | SR_AI | Наш проєкт |
|--------|-------|------------|
| **База даних** | SQLite (конвертовані CSV) | PostgreSQL (нативні таблиці) |
| **Тип пошуку** | По початку номера (`LIKE 'XXX%'`) | Точний пошук (`= 'XXX'`) |
| **Індексація** | B-tree в SQLite | B-tree + Hash в PostgreSQL |
| **Паралелізм** | По файлам SQLite | По таблицях PostgreSQL |
| **Оновлення** | Інкрементальна синхронізація CSV→SQLite | Інкрементальний імпорт CSV→PostgreSQL |

### Адаптація ідей SR_AI

**Що використовуємо з SR_AI:**
- ✅ Багатопоточний пошук (`ThreadPoolExecutor`)
- ✅ Кешування з'єднань до БД
- ✅ Batch обробка результатів
- ✅ Оптимізовані індекси для швидкого пошуку
- ✅ Система варіантів номерів справ

**Що адаптуємо для наших потреб:**
- 🔄 SQLite → PostgreSQL з hash індексами
- 🔄 Пошук по шаблону → Точний пошук конкретних справ
- 🔄 CSV конвертація → Використання існуючих таблиць `court_decisions_YYYY`

### Продуктивність

SR_AI досягає високої швидкості завдяки:
- **Індексованому доступу** - O(log n) замість O(n)
- **Паралельному пошуку** - одночасний пошук у всіх базах
- **Мінімальним запитам** - точні індексовані запити
- **Кешуванню** - повторне використання з'єднань

**Очікувана продуктивність після адаптації:** 10-50x прискорення пошуку завдяки оптимізованим PostgreSQL індексам та паралелізму.

## Система витягування кредиторів

### Двопроцесна архітектура

Проєкт використовує дві паралельні системи витягування кредиторів з резолютивних частин судових рішень:

#### **Процес 1: Основне витягування кредиторів**
- **API ключ**: `HeZDpzwyGunS0Vh6YHZ9JCvnSE81ntSM`
- **Модель**: `ministral-8b-latest`
- **Команда**: `python manage.py analyze_resolutions_mistral --continuous`
- **Функція**: Витягування нових кредиторів з найновіших резолютивних частин
- **Статистика**: Відслідковується через модель `LLMAnalysisLog`
- **Веб-управління**: Панель "Процес 1: Витягування кредиторів" на `/creditors/`

#### **Процес 2: Дедуплікація кредиторів**
- **API ключ**: `oyo1li90eL5R0yPmrlQ9OCUHwpvEmfKD`
- **Модель**: `ministral-8b-latest`
- **Команда**: `python manage.py analyze_resolutions_dedup --continuous`
- **Функція**: Виявлення та видалення дублікатів, додавання пропущених кредиторів
- **Статистика**: Відслідковується через модель `DeduplicationProcessStats`
- **Веб-управління**: Панель "Процес 2: Дедуплікація кредиторів" на `/creditors/`

### Алгоритм дедуплікації

**Принцип роботи:**
1. **Пріоритетний пошук**: Фокус на документах з ключовими словами дублікації:
   - "підсумками попереднього засідання"
   - "перераховуються"
   - "визнано грошові вимоги"
   - "повна версія"
   - "додатково"

2. **Розумне порівняння сум**: Використовує толерантність 1% для виявлення дублікатів
3. **Оновлення замість створення**: Якщо знайдено дублікат з меншою сумою, оновлює існуючу вимогу більшою сумою
4. **Типізація документів**: Розрізняє initial, full та summary документи для точної дедуплікації

**Статистика дедуплікації:**
- Справи оброблено
- Кредиторів додано
- Дублікатів видалено
- Вимог оновлено
- Розподіл по типах документів (initial/full/summary)

### Веб-інтерфейс кредиторів

**Сторінка `/creditors/`** містить:

**Дві панелі управління:**
- **Процес 1**: Зелена панель для основного витягування
- **Процес 2**: Синя панель для дедуплікації

**Автооновлення статистики:**
- Загальна статистика оновлюється кожні 5 секунд
- Статистика дедуплікації оновлюється паралельно
- AJAX запити без перезавантаження сторінки

**API ендпоінти:**
```bash
# Управління процесом витягування
POST /api/start-creditor-extraction/
POST /api/stop-creditor-extraction/

# Управління процесом дедуплікації
POST /api/start-deduplication-process/
POST /api/stop-deduplication-process/

# Отримання статистики
GET /creditors/?ajax=stats  # Загальна статистика
GET /api/deduplication-stats/  # Статистика дедуплікації
```

### Важливі особливості URLs

**⚠️ КРИТИЧНА ІНФОРМАЦІЯ**: Функція `case_detail` в `bankruptcy/views.py:332-341` обробляє URL `/case/<case_id>/` **двояко**:

1. **Спочатку пробує як `id`** (база даних автоінкрементний ключ)
2. **Якщо не знайдено, пробує як `number`** (порядковий номер справи з CSV файлу)

**Приклад:**
- `/case/177593/` → пошук по `BankruptcyCase.objects.get(id=177593)`
- `/case/77131/` → спочатку `id=77131` (не знайдено), потім `number=77131` (знайдено справу 922/1672/25)

**Діапазони значень:**
- **ID**: 176696-268227 (автоінкрементні ключі PostgreSQL)
- **Number**: 1-268227 (порядкові номери з CSV, можуть мати пропуски)

Це дозволяє використовувати як сучасні посилання (по ID), так і застарілі посилання (по номеру) без поломки функціоналу.

## Розробка

- Всі коментарі та докстрінги пишуються українською мовою
- Використовуй Django Best Practices
- Дотримуйся PEP 8 для Python коду
- Веб-інтерфейс повинен бути адаптивним та доступним
- Валідація ЄДРПОУ кодів обов'язкова при обробці даних підприємств
- При розробці інтерфейсу слідуй існуючим патернам стилізації