# 🏛️ Аналіз Клієнтів - Система аналізу справ про банкрутство

Система для обробки та аналізу даних про провадження у справах про банкрутство в Україні з автоматичним витягуванням судових рішень та кредиторів.

[![Python](https://img.shields.io/badge/Python-3.12-blue.svg)](https://www.python.org/)
[![Django](https://img.shields.io/badge/Django-5.2.6-green.svg)](https://www.djangoproject.com/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue.svg)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://www.docker.com/)

## 📋 Зміст

- [Швидкий старт](#-швидкий-старт)
- [Архітектура](#-архітектура)
- [Make команди](#-make-команди)
- [Функціональність](#-функціональність)
- [Розробка](#-розробка)
- [Документація](#-документація)


## 🚀 Швидкий старт

### Вимоги
- Docker 29.1+ та Docker Compose v5.0+
- Make
- 26+ GB вільного місця (для CSV даних судових рішень)

### Запуск за 3 команди

```bash
# 1. Клонувати репозиторій
git clone <repository-url>
cd analiz_klientiv

# 2. Створити .env файл
cp .env.example .env

# 3. Швидкий старт (збірка + запуск + міграції + імпорт)
make quickstart
```

Відкрийте http://localhost:8000 у браузері! 🎉

### Перший запуск вручну

Якщо потрібен контроль над кожним кроком:

```bash
# Збудувати Docker образи
make build

# Запустити контейнери
make up

# Застосувати міграції
make migrate

# Імпортувати довідкові дані та дані судових рішень
make import-all

# Переглянути логи
make logs
```

## 🏗️ Архітектура

### Docker контейнери (7 сервісів)

| Контейнер | Призначення | Порт |
|-----------|-------------|------|
| **analiz_db** | PostgreSQL 15 (база даних) | 5433 |
| **analiz_redis** | Redis 7 (кешування) | 6380 |
| **analiz_web** | Django development server | 8000 |
| **analiz_file_monitor** | Watchdog моніторинг CSV файлів | - |
| **analiz_stats_monitor** | Автооновлення статистики | - |
| **analiz_mistral_processor** | LLM витягування кредиторів | - |
| **analiz_dedup_processor** | LLM дедуплікація кредиторів | - |

### Ключові технології

- **Backend**: Django 5.2.6 + Django REST Framework
- **База даних**: PostgreSQL 15 (3.6M+ судових рішень)
- **Кеш**: Redis 7
- **AI**: Mistral AI API для витягування даних
- **Моніторинг**: Watchdog для автоматичного імпорту
- **Frontend**: Django Templates + Bootstrap 5 + AJAX

## 📦 Make команди

### Основні команди

```bash
make help              # Показати всі доступні команди
make quickstart        # Швидкий старт (all-in-one)
make status            # Статус контейнерів
```

### Управління контейнерами

```bash
make build             # Збудувати образи
make up                # Запустити контейнери
make down              # Зупинити контейнери
make restart           # Перезапустити контейнери
make clean             # Видалити контейнери та volumes
```

### Django команди

```bash
make migrate           # Застосувати міграції
make makemigrations    # Створити міграції
make shell             # Django shell
make createsuperuser   # Створити адміністратора
make collectstatic     # Зібрати статичні файли
```

### Перегляд логів

```bash
make logs              # Всі логи
make logs-web          # Django логи
make logs-db           # PostgreSQL логи
make logs-monitor      # File monitor логи
make logs-mistral      # Mistral processor логи
make logs-dedup        # Deduplication processor логи
```

### Імпорт даних

```bash
make import-all        # Імпортувати всі дані (рекомендовано)
make import-bankruptcy # Імпортувати справи банкрутства
make import-reference  # Імпортувати довідкові дані
make import-courts     # Імпортувати дані судів
```

### База даних

```bash
make db-shell          # PostgreSQL shell
make backup-db         # Створити backup бази даних
make restore-db FILE=  # Відновити з backup
```

### Production

```bash
make build-prod        # Збудувати production образи
make up-prod           # Запустити production (з Nginx)
make down-prod         # Зупинити production
make logs-prod         # Production логи
```

## ✨ Функціональність

### 📊 Головна сторінка (/)
- **Загальна статистика**: Всього справ, нових за період, завершених, активних
- **Статистика за 6 місяців**: Помісячна динаміка справ
- **ТОП-5 Судів**: Інтерактивна карусель з фільтрацією
- **Без вертикального скролу**: Адаптивний дизайн

### 🏛️ Судові рішення (/court-decisions/)
**Ієрархічна навігація (4 рівні):**
1. **Роки** → 2. **Види судочинства** → 3. **Інстанції** → 4. **Форми рішень**

- 3.6M+ судових рішень (2008-2025)
- 830 судів, 5 видів судочинства, 7 інстанцій
- Інтелектуальне кешування (автооновлення кожні 5 хв)
- Експорт до CSV

### 📋 Справи про банкрутство (/cases/)
- Список справ з пагінацією (50/100/200 на сторінку)
- Фільтрація за судом, типом, датою
- Пошук за номером справи, ЄДРПОУ, назвою
- Детальна інформація про справу

### 💰 Кредитори (/creditors/)
**Автоматичне витягування кредиторів з резолютивних частин:**
- **Процес 1**: Витягування нових кредиторів (Mistral AI)
- **Процес 2**: Дедуплікація та оновлення сум
- Веб-панель управління процесами
- Статистика в реальному часі

### 🔍 Пошук судових рішень
- Автоматичний пошук при відкритті справи
- Багаторічні бази даних (2008-2025)
- Розумний пошук за варіантами номера
- Витягування резолютивних частин

### ⚙️ Автоматизація

**File Monitor (watchdog):**
- Автоматичний імпорт при зміні CSV файлів
- Інкрементальне оновлення (тільки нові записи)
- Автоматичний пошук судових рішень для нових справ

**Stats Monitor:**
- Автооновлення статистики кожні 5 хвилин
- Мінімум 100 змін для тригера
- Видалення застарілих кешів

## 🛠️ Розробка

### Структура проєкту

```
analiz_klientiv/
├── docker/                      # Docker конфігурації
│   ├── django/Dockerfile        # Django multi-stage build
│   └── nginx/                   # Nginx конфігурації (production)
├── bankruptcy/                  # Django застосунок
│   ├── models.py                # Моделі (справи, суди, рішення, кредитори)
│   ├── views.py                 # Views з API та веб-інтерфейсом
│   ├── management/commands/     # Django management команди
│   └── templates/               # HTML шаблони
├── data/                        # CSV дані (26+ GB)
│   ├── Відомості про справи про банкрутство.csv
│   ├── documents_XX.csv         # Судові рішення по роках
│   ├── courts.csv               # Довідник судів
│   └── *.csv                    # Інші довідкові дані
├── static/                      # Статичні файли (CSS, JS)
├── templates/                   # Глобальні шаблони
├── docker-compose.yml           # Development конфігурація
├── docker-compose.prod.yml      # Production конфігурація
├── Makefile                     # Make команди (60+)
├── CLAUDE.md                    # Інструкції для Claude Code
├── README.md                    # Ця документація
└── requirements.txt             # Python залежності

Еталонні проєкти (тільки для читання):
├── project_courtagent.pro_production/  # Робочий приклад
└── SR_AI/                              # Високопродуктивний пошук
```

### Ключові моделі даних

**BankruptcyCase** - Справи про банкрутство
- `number`, `case_number`, `date`, `type`
- `company` (ForeignKey), `court` (ForeignKey)
- `start_date_auc`, `end_date_auc`

**CourtDecision (по роках: court_decisions_YYYY)**
- PostgreSQL таблиці (не Django моделі)
- `doc_id`, `cause_num`, `court_code`
- `justice_kind`, `judgment_code`
- `adjudication_date`, `judge`
- `resolution_text` (витягнута резолютивна частина)

**Creditor / CreditorClaim** - Кредитори
- Автоматично витягуються з резолютивних частин
- `creditor_name`, `claim_amount`, `claim_type`
- `bankruptcy_case` (ForeignKey)

**CourtDecisionStatistics** - Кешована статистика
- `stat_type`: general, yearly, hierarchical, recent, courts
- `stat_data`: JSON з результатами
- `cache_expires_at`: автоматичне оновлення

### Налаштування середовища

**Development (.env):**
```bash
DEBUG=True
POSTGRES_HOST=db
POSTGRES_PORT=5432
REDIS_HOST=redis
```

**Production (.env.production):**
```bash
DEBUG=False
SECRET_KEY=<сгенерований ключ>
ALLOWED_HOSTS=your-domain.com
POSTGRES_PASSWORD=<безпечний пароль>
MISTRAL_API_KEY_PROCESS1=<ключ 1>
MISTRAL_API_KEY_PROCESS2=<ключ 2>
```

### Додавання нових CSV файлів

1. Розмістити файл в `data/`
2. File monitor автоматично виявить зміни
3. Або запустити вручну: `make import-bankruptcy`

### Тестування

```bash
make test              # Запустити тести
make test-coverage     # Тести з покриттям
make shell             # Django shell для дебагу
```

## 📚 Документація

### Детальна документація

- **[CLAUDE.md](CLAUDE.md)** - Повна документація проєкту для Claude Code
  - Технологічний стек
  - Структура даних
  - Команди розробки
  - Архітектура SR_AI
  - Двопроцесна система витягування кредиторів
  - Systemd служби

- **[DOCKER_README.md](DOCKER_README.md)** - Docker deployment guide (якщо створений)

### API Endpoints

```bash
# Ієрархічна статистика судових рішень
GET /api/court-decisions/hierarchical-stats/
    ?year=2025
    &justice_kind=3
    &instance_code=1
    &judgment_form=2
    &force_refresh=true

# Загальна статистика
GET /api/court-decisions/stats/?type=general

# Управління процесами витягування
POST /api/start-creditor-extraction/
POST /api/stop-creditor-extraction/
POST /api/start-deduplication-process/
POST /api/stop-deduplication-process/

# Статистика дедуплікації
GET /api/deduplication-stats/
```

### Troubleshooting

**Порти зайняті:**
```bash
# Змінити порти в docker-compose.yml
# PostgreSQL: 5433:5432 (замість 5432:5432)
# Redis: 6380:6379 (замість 6379:6379)
```

**Контейнери не запускаються:**
```bash
make logs              # Перевірити логи
make down && make up   # Перезапустити
make clean && make build  # Повне перебудування
```

**База даних порожня:**
```bash
make import-all        # Імпортувати всі дані
make db-shell          # Перевірити БД вручну
```

**Статистика не відображається:**
```bash
# Очистити кеш
docker exec analiz_web python manage.py shell -c "
from bankruptcy.models import CourtDecisionStatistics
CourtDecisionStatistics.objects.all().delete()
"

# Або через веб-інтерфейс натиснути кнопку 🔄 (force refresh)
```

## 🔒 Безпека

**Production чеклист:**
- ✅ Змінити `SECRET_KEY` в `.env.production`
- ✅ Встановити `DEBUG=False`
- ✅ Налаштувати `ALLOWED_HOSTS`
- ✅ Використовувати безпечні паролі для PostgreSQL
- ✅ Налаштувати SSL/TLS в Nginx
- ✅ Обмежити доступ до портів (firewall)
- ✅ Регулярні backup бази даних (`make backup-db`)

## 📊 Продуктивність

### Оптимізації

- **Індекси PostgreSQL**: B-tree + Hash індекси на всіх ключових полях
- **Багатопоточність**: 15 воркерів для пошуку та обробки
- **Кешування**: Redis + CourtDecisionStatistics (1-12 годин TTL)
- **Batch обробка**: 300-400 записів на батч
- **Connection pooling**: Повторне використання з'єднань
- **Nginx**: Gzip, статичні файли, keepalive

### Метрики

- **Пошук судових рішень**: ~30ms на запит
- **Ієрархічна статистика**: 20-100ms (з кешу: 2-5ms)
- **Витягування резолютивних частин**: ~2-5s на документ
- **Обробка LLM**: ~3-7s на кредитора

## 🤝 Внесок

Для розробників:

1. Fork репозиторію
2. Створити feature branch: `git checkout -b feature/amazing-feature`
3. Commit зміни: `git commit -m 'Add amazing feature'`
4. Push в branch: `git push origin feature/amazing-feature`
5. Створити Pull Request

## 📝 Ліцензія

[Вкажіть ліцензію проєкту]

## 👥 Автори

[Вкажіть авторів]

## 🙏 Подяки

- Django та Django REST Framework команди
- PostgreSQL community
- Mistral AI за API для витягування даних
- Bootstrap за UI компоненти

---

**Версія**: 1.0.0
**Остання оновлення**: Грудень 2025
**Python**: 3.12 | **Django**: 5.2.6 | **PostgreSQL**: 15